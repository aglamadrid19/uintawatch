# UintaWatch teaser — voiceover script (video is silent, 60.0s)

Cues are mm:ss from video start. The video's own caption text carries the
details — the VO stays sparse and lands just after each cut. ~120 words.
The one quick line is 0:44 (typing beat); everything else can breathe.

| Time | On screen | Line |
|------|-----------|------|
| 0:00 | Title: UintaWatch — open field lab | **UintaWatch — an open field lab for low-cost wildfire sensing.** |
| 0:04 | "Utah is burning." stats | **Utah is burning. A million acres since 2015. Three-quarters — human-caused.** |
| 0:08 | Live map, pulsing nodes | **Eight nodes on one map — a simulation of the mesh we're building.** |
| 0:12 | Sensor sheet springs open | **Every node, one tap away.** |
| 0:19 | Node detail: readings | **Battery, wind, mesh health — you can see what a node needs before it fails.** |
| 0:24 | Alert detail | **Alerts explain themselves: what fired, why, and which node corroborates.** |
| 0:30 | Agent tab, question typed | **Or just ask the agent.** |
| 0:34 | Reply streams in | **It answers from the same data the map sees — and admits it's only a simulation.** |
| 0:40 | Report CTA + empty form | **See smoke? Report it.** |
| 0:44 | Typing (quick beat) | **Type what you see.** |
| 0:46 | Success dialog | **It persists offline until the backend exists.** |
| 0:50 | uintawatch.com browser card | **Data, failures, receipts — all published at uintawatch.com.** |
| 0:54 | End card | **The ridges are watching. Now they keep a lab notebook, too.** |

## Plain read (no table)

UintaWatch — an open field lab for low-cost wildfire sensing.

Utah is burning. A million acres since 2015. Three-quarters — human-caused.

Eight nodes on one map — a simulation of the mesh we're building.

Every node, one tap away.

Battery, wind, mesh health — you can see what a node needs before it fails.

Alerts explain themselves: what fired, why, and which node corroborates.

Or just ask the agent.

It answers from the same data the map sees — and admits it's only a simulation.

See smoke? Report it.

Type what you see.

It persists offline until the backend exists.

Data, failures, receipts — all published at uintawatch.com.

The ridges are watching. Now they keep a lab notebook, too.

## Read notes
- Pause ~0.5s after each cut; the caption on screen does the explaining.
- 0:30 "Or just ask the agent." — deadpan; the dots are spinning on screen.
- 0:44 "Type what you see." — the segment is only ~2s; say it briskly.
- 0:54 close — read it like the card says it. Then let the last 3s run silent.
